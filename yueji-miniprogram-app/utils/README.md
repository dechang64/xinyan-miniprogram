# Utils 目录

工具函数 (严守预审, 数据格式化, 本地缓存等)。

v1.1 接入:
- 严守预审: `_validatePrompt()` / `_validateText()`
- 日期格式化: `formatDate()` / `dayOfYear()`
- 本地缓存: `getStorage()` / `setStorage()` / `clearStorage()`
- 网络请求: `cloudCall()` (v1.1 接 CloudBase)
- 音频播放: `playMusic()` (v1.1 接 5 段曲风真实 URL)
