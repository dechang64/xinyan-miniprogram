<view class="page">
  <view class="header">
    <view class="title-main">写给 3 个月后的自己</view>
    <view class="title-sub" wx:if="{{savedAt}}">保存于 {{savedAt}}</view>
  </view>

  <view class="card">
    <textarea
      class="letter-textarea"
      placeholder="此刻, 你想对 3 个月后的自己说些什么?&#10;想写多长都行, 想什么时候改都行。"
      value="{{letter}}"
      bindinput="onInput"
      maxlength="-1"
      auto-height
    />
  </view>

  <view class="action-row">
    <view class="btn-primary" bindtap="onSave">保存</view>
    <view class="btn-ghost" bindtap="onClear">清除</view>
  </view>

  <view class="compliance-bar">
    ✦ 这封信只存在你手机本地, 关 App 不清, 但清除操作可随时删除。
  </view>
</view>
